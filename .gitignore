# Ignore everything by default
/*

# But upload these things from the root only
!/docs
!/.gitignore
!/index.md
!/package-lock.json
!/package.json

# And don't upload that, no matter where they are
node_modules
.temp
.cache
.idea
.DS_Store
yarn.lock